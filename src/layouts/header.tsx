import { BarreNavigation } from "components/header/barreNavigation";
import { FC } from "react";

// Composant Header qui rend la barre de navigation
export const Header: FC = () => {
  return <BarreNavigation />;
};
